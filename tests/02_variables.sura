# 변수 할당 및 복합 대입
x is 10
x += 5
assert_eq(x, 15)
x -= 3
assert_eq(x, 12)
x *= 2
assert_eq(x, 24)
x /= 4
assert_eq(x, 6)
x %= 4
assert_eq(x, 2)

# 문자열 변수
s is "hello"
s is s + " world"
assert_eq(s, "hello world")

print "02_variables: PASS"
